#!/usr/bin/env python3
"""Surgical fixes for LECTURE_SPEECH.md layered format."""

from __future__ import annotations

import re
from pathlib import Path

SPEECH = Path(__file__).resolve().parents[1] / "LECTURE_SPEECH.md"

REPLACEMENTS = [
    (
        """[РЕЧЬ]
Давайте честно посмотрим, как ведёт себя типичное приложение, когда сеть пропадает. Один из трёх вариантов: белый экран, бесконечный спиннер или надпись «Нет соединения, попробуйте позже». И вот это «попробуйте позже» — это мы расписываемся в том, что переложили свою проблему на пользователя.
`[СЛАЙД: «Сеть — это ненадёжный внешний ресурс, а не данность»]`""",
        """[РЕЧЬ]
Давайте честно посмотрим, как ведёт себя типичное приложение, когда сеть пропадает. Один из трёх вариантов: белый экран, бесконечный спиннер или надпись «Нет соединения, попробуйте позже». И вот это «попробуйте позже» — это мы расписываемся в том, что переложили свою проблему на пользователя.

`[ВОПРОС В ЗАЛ]` Поднимите руку, у кого хоть раз в метро не открывался билет или не грузилась карта. `[ПАУЗА]` Вот. И это не потому, что разработчик плохой. Это потому, что в голове была одна неявная и очень опасная установка.

`[СЛАЙД: «Сеть — это ненадёжный внешний ресурс, а не данность»]`""",
    ),
    (
        """[РЕЧЬ]
План такой. Блок 1 — кэширование и, самое важное, инвалидация: как показывать данные без сети и при этом не показывать протухшее и не утечь чужое. Блок 2 — offline-first: локальный источник правды, офлайн-билет и очередь отложенных действий. Блок 3 — безопасность: где и как хранить данные, Keychain, папки песочницы, FaceID. И блок 4 — итоги, оффлайн-оплата по BLE на словах и чек-лист.
Вот наша отправная точка. Это кошелёк: онлайн-лента, билет с QR-кодом, баланс. Всё грузится напрямую из сети. Красиво работает — пока есть интернет.
`[СЛАЙД: «Откуда данные — два mock API + маппинг на клиенте»]`""",
        """[РЕЧЬ]
План такой. Блок 1 — кэширование и, самое важное, инвалидация: как показывать данные без сети и при этом не показывать протухшее и не утечь чужое. Блок 2 — offline-first: локальный источник правды, офлайн-билет и очередь отложенных действий. Блок 3 — безопасность: где и как хранить данные, Keychain, папки песочницы, FaceID. И блок 4 — итоги, оффлайн-оплата по BLE на словах и чек-лист.

`[КОММИТ 1]` `[СЛАЙД: структура приложения — лента, билет, баланс]`

[РЕЧЬ]
Вот наша отправная точка. Это кошелёк: онлайн-лента, билет с QR-кодом, баланс. Всё грузится напрямую из сети. Красиво работает — пока есть интернет.

`[СЛАЙД: «Откуда данные — два mock API + маппинг на клиенте»]`""",
    ),
    (
        """А вот что происходит в авиарежиме. Я специально записал это видео заранее, не буду запускать вживую — демо с реальной сетью на сцене это лотерея. Смотрите: лента — экран ошибки, билет — `wifi.slash`, QR не строится. Пассажир в самолёте остался без посадочного. Наша цель к концу лекции — чтобы ровно этот же экран в авиарежиме работал. Поехали.
---""",
        """А вот что происходит в авиарежиме. Я специально записал это видео заранее, не буду запускать вживую — демо с реальной сетью на сцене это лотерея. Смотрите: лента — экран ошибки, билет — `wifi.slash`, QR не строится. Пассажир в самолёте остался без посадочного. Наша цель к концу лекции — чтобы ровно этот же экран в авиарежиме работал. Поехали.

---""",
    ),
    (
        """`[КОММИТ 2]` `[СЛАЙД: «URLCache — почти бесплатный HTTP-кэш»]`

Первый и самый дешёвый шаг к отказоустойчивости — это кэш сетевых ответов. И тут хорошая новость: в Foundation уже встроен механизм, который делает 80% работы за нас. Называется `URLCache`.

`[СЛАЙД: схема — твой код → URLSession → [URLCache?] → сеть]`

[НА СЛАЙДЕ]
`[КОД]` `[СЛАЙД: подключение URLCache]`

[РЕЧЬ]
Идея простая. `URLCache` — это прослойка между вашим кодом и сетью. Вы делаете обычный запрос через `URLSession`, а система сама решает: сходить в сеть или отдать сохранённый ответ. Прозрачно для кода — вы пишете тот же `session.data(for: request)`, что и всегда.
```swift
let cache = URLCache(
    memoryCapacity: 16 * 1024 * 1024,   // 16 MB RAM
    diskCapacity: 128 * 1024 * 1024,    // 128 MB disk
    diskPath: "api-cache"
)
let config = URLSessionConfiguration.default
config.urlCache = cache
config.requestCachePolicy = .useProtocolCachePolicy
```
Здесь два уровня. **Memory** — быстрый кэш в оперативке, живёт пока запущено приложение. **Disk** — переживает перезапуск; физически лежит в `Library/Caches`. Запомните эту папку — мы к ней ещё трижды вернёмся, потому что ОС вправе вычистить её под нехватку места. При превышении ёмкости старые записи вытесняются сами.
Теперь — самое важное в этом подразделе, и то, что многие iOS-разработчики знают смутно. **Кэшем управляет не клиент. Кэшем управляет сервер — через HTTP-заголовки.** Давайте разберём, как именно, потому что без этого `URLCache` кажется магией, а магию страшно тащить в продакшн.
`[СЛАЙД: «Два вопроса кэша: свежесть и валидация»]`""",
        """`[КОММИТ 2]` `[СЛАЙД: «URLCache — почти бесплатный HTTP-кэш»]`

[РЕЧЬ]
Первый и самый дешёвый шаг к отказоустойчивости — это кэш сетевых ответов. И тут хорошая новость: в Foundation уже встроен механизм, который делает 80% работы за нас. Называется `URLCache`.

`[СЛАЙД: схема — твой код → URLSession → [URLCache?] → сеть]`

[НА СЛАЙДЕ]
код → URLSession → URLCache? → сеть

[РЕЧЬ]
Идея простая. `URLCache` — это прослойка между вашим кодом и сетью. Вы делаете обычный запрос через `URLSession`, а система сама решает: сходить в сеть или отдать сохранённый ответ. Прозрачно для кода — вы пишете тот же `session.data(for: request)`, что и всегда.

`[КОД]` `[СЛАЙД: подключение URLCache]`

[НА СЛАЙДЕ]
```swift
let cache = URLCache(
    memoryCapacity: 16 * 1024 * 1024,   // 16 MB RAM
    diskCapacity: 128 * 1024 * 1024,    // 128 MB disk
    diskPath: "api-cache"
)

let config = URLSessionConfiguration.default
config.urlCache = cache
config.requestCachePolicy = .useProtocolCachePolicy
```

[РЕЧЬ]
Здесь два уровня. **Memory** — быстрый кэш в оперативке, живёт пока запущено приложение. **Disk** — переживает перезапуск; физически лежит в `Library/Caches`. Запомните эту папку — мы к ней ещё трижды вернёмся, потому что ОС вправе вычистить её под нехватку места. При превышении ёмкости старые записи вытесняются сами.

Теперь — самое важное в этом подразделе, и то, что многие iOS-разработчики знают смутно. **Кэшем управляет не клиент. Кэшем управляет сервер — через HTTP-заголовки.** Давайте разберём, как именно, потому что без этого `URLCache` кажется магией, а магию страшно тащить в продакшн.

`[СЛАЙД: «Два вопроса кэша: свежесть и валидация»]`""",
    ),
]


def shorten_slide_bullets(text: str) -> str:
    """Shorten overly long bullets on slides for compact PPTX."""
    subs = {
        "- `Cache-Control: max-age=3600` — основной механизм. Ответ считается свежим 3600 секунд, то есть час, с момента получения. Весь этот час `URLSession` отдаёт его из кэша, не трогая сеть.":
        "- `Cache-Control: max-age=N` — свежесть N секунд, без сети",
        "- `Cache-Control: no-store` — «не сохранять вообще»: ни в память, ни на диск. Это для чувствительных данных — балансов, персональной информации. Запомните на блок безопасности.":
        "- `no-store` — не сохранять (балансы, PII)",
        "- `Cache-Control: no-cache` — внимание, обманчивое имя. Это **не** «не кэшировать». Это «сохрани, но никогда не отдавай без перепроверки на сервере». То есть всегда делай условный запрос.":
        "- `no-cache` — сохранить, но всегда revalidate",
        "- `Cache-Control: private` против `public` — кэшировать только на клиенте или можно где угодно, включая прокси и CDN.":
        "- `private` / `public` — клиент vs прокси/CDN",
        "- `Expires: <дата>` — устаревший абсолютный аналог `max-age`. Если присутствуют оба, выигрывает `max-age`.":
        "- `Expires` — legacy, проигрывает `max-age`",
        "- `ETag: \"abc123\"` в ответе ↔ `If-None-Match: \"abc123\"` в запросе. `ETag` — это «отпечаток», версия ресурса. Точный механизм: реагирует на любое изменение байт.":
        "- `ETag` ↔ `If-None-Match` — точная версия ресурса",
        "- `Last-Modified: <дата>` в ответе ↔ `If-Modified-Since: <дата>` в запросе. То же самое, но по дате. Проще, но грубее: разрешение одна секунда.":
        "- `Last-Modified` ↔ `If-Modified-Since` — по дате",
        "- **`304 Not Modified`** — «не менялось, бери из кэша». Тело не передаётся, только заголовки. `URLSession` сам достаёт тело из своего кэша и отдаёт его вам как обычный успешный ответ. В коде вы даже не видите, что был 304 — вы просто получаете данные.":
        "- `304 Not Modified` — тело из кэша, ~0 трафика",
        "- **`200 OK`** с новым телом и новым `ETag` — ресурс изменился, вот свежая версия.":
        "- `200 OK` — новое тело + новый ETag",
    }
    for old, new in subs.items():
        text = text.replace(old, new)
    return text


def main() -> None:
    text = SPEECH.read_text(encoding="utf-8")
    for old, new in REPLACEMENTS:
        if old not in text:
            print(f"⚠ pattern not found ({old[:50]}…)")
        else:
            text = text.replace(old, new, 1)
    text = shorten_slide_bullets(text)
    SPEECH.write_text(text, encoding="utf-8")
    print(f"✓ Patched {SPEECH}")


if __name__ == "__main__":
    main()
